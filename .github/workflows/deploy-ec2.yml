name: Deploy to EC2 on master

on:
  push:
    branches:
      - master  # master로 머지/푸시될 때만 실행

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy to EC2 via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            # 프로젝트 디렉토리로 이동
            cd /home/ubuntu/services/Back_EvaludationManagementSystem
            
            # Git 최신 코드 가져오기
            echo "📥 Fetching latest code from master..."
            git fetch origin
            git reset --hard origin/master
            
            # 최신 커밋 정보 출력
            echo "📌 Current commit: $(git log -1 --oneline)"
            
            # Node.js 의존성 설치
            echo "📦 Installing dependencies..."
            npm ci --production=false
            
            # 프로젝트 빌드
            echo "🔨 Building project..."
            npm run build
            
            # PM2로 서버 재시작 (없으면 시작, 있으면 재시작)
            echo "🚀 Restarting application with PM2..."
            pm2 restart lumir-evaluation-system || pm2 start dist/src/main.js --name lumir-evaluation-system
            
            # PM2 설정 저장
            pm2 save
            
            # 배포 상태 확인
            echo "✅ Deployment completed!"
            echo "📊 PM2 Status:"
            pm2 list
            pm2 logs lumir-evaluation-system --lines 5 --nostream || true

